#include <iostream>
#include "./include/orderedhttest.hpp"

int main ()
{
    std::string command = "";
    if (command == "m")
    {
        int N;
        std::cin >> N;
    }
    while (command != "exit")
    {
        std::cin >> command;
        if (command == "i")
        {

        } else if (command == "s")
        {

        } else if (command == "d")
        {

        } else if (command == "p")
        {

        }
    }
}
